;-- Intentionally empty.
;-- Dux's Innumerable Characters Kit already declares [stationary_squads] in its
;-- own offline_combat_simulator.ltx. This empty file overrides the base Warfare
;-- stationary file (mod_offline_combat_simulator_warfare_stationary.ltx) so the
;-- section is not declared twice (a duplicate [section] declaration would error).
;-- Select this option when Dux is enabled; deselect it if Dux is absent.
